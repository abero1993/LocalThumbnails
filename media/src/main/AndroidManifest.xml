<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.btx.abero.media">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".FileListActivity"
            android:theme="@style/MainTheme"></activity>
        <activity
            android:name=".VideoActivity"
            android:theme="@style/VideoPlayerTheme"></activity>
        <activity android:name=".AudioPlayActivity"></activity>
        <activity android:name=".PicViewActivity"></activity>

    </application>

</manifest>